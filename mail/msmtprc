# Set default values for all following accounts.
defaults
auth           on
tls            on
tls_certcheck  off
#tls_trust_file /etc/ssl/ca-certificates.crt
logfile        ~/.msmtp.log

# Gmail
account        protonmail
host           localhost
port           1025
from           mail
user           maiil
password       secret

# Set a default account
account default : protonmail
